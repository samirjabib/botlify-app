# landing with Chat Box-GPT

Landing Page with Chat Box-GPT

## How to run:

Clone the project, then we recommend ppnpm to install and run the project.

```bash
pnpm install
```

Then:

```bash
pnpm dev
```
