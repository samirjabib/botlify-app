import { Title } from "@/design-system/index";

const BlogContent = ({}) => {
  return (
    <div>
      <Title as="h1" size={"mainTitle"}>
        Blog Content
      </Title>
    </div>
  );
};

export default BlogContent;
